package com.example.books;

public class BookRepository{
    // 1. We need a BookRepository class to save the book to the database.
    // The BookRepository class must have the following method: save(Book book).
    // It must also be able to handle database connections.
}
